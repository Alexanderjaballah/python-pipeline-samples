pipeline:
  identifier: Build_python_pipeline_samples_1718356637998
  name: Build python-pipeline-samples
  orgIdentifier: default
  projectIdentifier: Python
  properties:
    ci:
      codebase:
        build: <+input>
        connectorRef: account.github_harness
        repoName: python-pipeline-samples
  stages:
    - stage:
        identifier: build
        name: build
        spec:
          caching:
            enabled: true
          cloneCodebase: true
          execution:
            steps:
              - step:
                  type: Run
                  identifier: dependencies
                  name: Dependencies
                  spec:
                    shell: Sh
                    command: |-
                      python -m pip install --upgrade pip
                      pip install -r requirements.txt
                    envVariables:
                      PIP_CACHE_DIR: /root/.cache
          platform:
            arch: Amd64
            os: Linux
          runtime:
            spec: {}
            type: Cloud
        type: CI
